<div class="bg-gray-50">
    <!-- Navbar -->
    <nav class="bg-white shadow-lg fixed w-full z-50 top-0 transition-all duration-300 ease-in-out">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <!-- Logo -->
                <div class="flex-shrink-0">
                    <a href="#" class="flex items-center">
                        <i class="fas fa-image text-red-500 text-2xl"></i>
                        <span class="ml-2 text-xl font-bold lily text-gray-900">Image Hosting</span>
                    </a>
                </div>

                <!-- Desktop Menu -->
                <div class="hidden md:flex items-center space-x-8">
                    <a href="#" class="text-gray-600 hover:text-blue-600 px-3 py-2 text-sm font-medium transition duration-300 ease-in-out">Home</a>
                    <a href="#" class="text-gray-600 hover:text-blue-600 px-3 py-2 text-sm font-medium transition duration-300 ease-in-out">About</a>
                    <a href="#" class="text-gray-600 hover:text-blue-600 px-3 py-2 text-sm font-medium transition duration-300 ease-in-out">Contact</a>
                </div>

                <!-- CTA and Mobile Menu Button -->
                <div class="flex items-center space-x-4">
                    <a href="#" class="hidden md:inline-block px-6 py-2 bg-blue-600 text-white font-medium text-sm rounded-full hover:bg-blue-700 transition duration-300 ease-in-out shadow-md">
                        Get Started
                    </a>
                    <button id="mobile-menu-btn" class="md:hidden p-2 rounded-lg text-gray-600 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-300" aria-label="Toggle mobile menu">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <!-- Mobile Menu -->
        <div id="mobile-menu" class="md:hidden hidden bg-white border-t border-gray-200 shadow-sm">
            <div class="px-4 py-4 space-y-2">
                <a href="#" class="block px-4 py-2 text-gray-600 hover:bg-gray-100 hover:text-blue-600 rounded-lg text-sm font-medium transition duration-200">Home</a>
                <a href="#" class="block px-4 py-2 text-gray-600 hover:bg-gray-100 hover:text-blue-600 rounded-lg text-sm font-medium transition duration-200">About</a>
                <a href="#" class="block px-4 py-2 text-gray-600 hover:bg-gray-100 hover:text-blue-600 rounded-lg text-sm font-medium transition duration-200">Contact</a>
                <a href="#" class="block px-4 py-2 bg-blue-600 text-white font-medium text-sm rounded-lg hover:bg-blue-700 transition duration-200">Get Started</a>
            </div>
        </div>
    </nav>

    <!-- Spacer to prevent content overlap with fixed navbar -->
    <div class="h-16"></div>

    <script>
        // Mobile menu toggle functionality
        const mobileMenuButton = document.getElementById('mobile-menu-btn');
        const mobileMenu = document.getElementById('mobile-menu');

        mobileMenuButton.addEventListener('click', function() {
            mobileMenu.classList.toggle('hidden');
        });
    </script>
</div>
